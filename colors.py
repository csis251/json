def get_color(color_name):
    """This functions returns a color value
        If color not found, returns None.
        TODO: Read from colors.json and return matching color value. Return None otherwise
        HINT: Create a separate function to read and parse the colors so that
              you can reuse that function in the color_setter function
    """
    pass


def set_color(color_name, color_value):
    """This function allows the user to update a color
        TODO: Overwrite colors.json file, adding the new color name and value
    """
    pass
