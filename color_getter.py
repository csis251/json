def get_color():
    """This function asks the user for a color name, and returns a color code
        1) Ask the user for a color
        2) Read colors from colors.json
        3a) If color found, print: <color-name>: <color-value>. E.g.:
            For "aliceblue", print:
                aliceblue: [240, 248, 255, 1]
        3b) If color not found: print nothing
        4) return color value (return None if color not found)
    """
    pass


if __name__ == "__main__":
    get_color()
